<Window x:Class="PL.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"        
        Title="Login" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="myLableStyle" TargetType="Label">
            <Setter Property="FontSize"
             Value="12" />
            <Setter Property="FontFamily"
             Value="Consolas" />
            <Setter Property="FontWeight"
             Value="Bold" />
        </Style>
        <Style x:Key="myButtonStyle" TargetType="Button">
            <Setter Property="FontSize"
                Value="32" />
            <Setter Property="FontFamily"
                 Value="Consolas" />
            <Setter Property="FontWeight"
                 Value="Bold" />
        </Style>
    </Window.Resources>
    <Grid>
        <Button Style="{StaticResource myButtonStyle}" Content="Customer" Click="btnPressCustomer_Click" Margin="137,288,374,67"/>
        <Button Style = "{StaticResource myButtonStyle}" Content="Worker" Margin="137,70,374,284" />
        <Button Style = "{StaticResource myButtonStyle}" Content="New Customer" Click="btnNewCustomer_Click" Margin="133,166,378,188"/>

    </Grid>
</Window>
